{
  "name": "@catering/mobile",
  "version": "0.1.0",
  "private": true,
  "description": "אפליקציית מובייל Expo (25)",
  "scripts": {
    "dev": "expo start --port 8081",
    "start": "expo start",
    "android": "expo run:android",
    "ios": "expo run:ios",
    "build": "eas build --platform all",
    "submit": "eas submit",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@catering/contracts": "workspace:*",
    "@catering/utils": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.10.0",
    "vitest": "^2.1.8"
  }
}

{
  "name": "expenses",
  "version": "1.0.0",
  "private": true,
  "description": "Expenses & Budget Management — backend + frontend",
  "workspaces": ["backend", "frontend"],
  "scripts": {
    "dev": "echo 'Run separately: npm --workspace backend run dev / npm --workspace frontend run dev'",
    "dev:backend": "npm --workspace backend run dev",
    "dev:frontend": "npm --workspace frontend run dev"
  }
}

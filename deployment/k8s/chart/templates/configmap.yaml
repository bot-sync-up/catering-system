apiVersion: v1
kind: ConfigMap
metadata:
  name: platform-config
data:
  NODE_ENV: production
  LOG_LEVEL: info
  RATE_LIMIT_RPS: "30"
  ENABLE_AUDIT_LOG: "true"
  DOMAIN: {{ .Values.global.domain | quote }}
  NEXT_TELEMETRY_DISABLED: "1"

{{- range $app := .Values.apps }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ $app.name }}
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: {{ $app.name }}
  ports:
    - name: http
      port: 80
      targetPort: 3000
{{- end }}
